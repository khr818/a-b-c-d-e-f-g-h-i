// prisma/schema.prisma
// Define your database models

generator client {
  provider = "prisma-client"
  output   = "postgres://e502b4c066a16221e49922e9e71a7fcdba8ce01e3138583eda4fb2fa10c35ff5:sk_ajaFwjJj8nUGZmS9p_mAN@db.prisma.io:5432/postgres?sslmode=require"
}

datasource db {
  provider = "postgresql"
}

// Example models

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
  posts Post[]
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    User    @relation(fields: [authorId], references: [id])
  authorId  Int
}
